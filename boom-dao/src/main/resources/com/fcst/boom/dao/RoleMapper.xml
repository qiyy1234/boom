<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.fcst.boom.dao.RoleDao" >
  <resultMap id="roleResult" type="Role">
		<id property="id" column="id" />
		<result property="name" column="name" />
		<result property="enname" column="enname" />
		<result property="dataScope" column="dataScope" />
		<result property="createBy" column="createBy" />
		<result property="createDate" column="create_date" />
	</resultMap>
	
	<sql id="roleColumns">
    	r.id,
    	r.name,
    	r.enname,
		r.data_scope AS dataScope,
		r.create_by AS createBy,
		r.create_date,
    </sql>
  
  	<select id="findList" resultMap="roleResult">
		SELECT 
			r.id,
    		r.name,
    		r.enname,
			r.data_scope AS dataScope,
			r.create_by AS createBy,
			r.create_date
		FROM sys_role r
	</select>
	
	<insert id="addRole" parameterType="com.fcst.boom.dao.RoleDao">
		INSERT INTO sys_role (id,name,enname,role_type,data_scope,is_sys,useable,create_by,create_date,update_by,update_date,del_flag) 
		values (#{id},#{name},#{enname},#{roleType},'1',#{sysData},#{useable},#{createBy},#{createDate},#{createBy},#{createDate},0)
	</insert>
	
	<select id="detailRole" resultMap="roleResult">
		SELECT 
			r.id,
    		r.name,
    		r.enname,
			r.data_scope AS dataScope,
			r.create_by AS createBy,
			r.create_date
		FROM sys_role r
		WHERE r.id = #{id}
	</select>
		
</mapper>
